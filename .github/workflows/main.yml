name: Run Windows RDP via Port 443

on: [workflow_dispatch]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Configure Windows User and RDP
        shell: powershell
        run: |
          $Password = ConvertTo-SecureString "WindowsPassword123!" -AsPlainText -Force
          Set-LocalUser -Name "runneradmin" -Password $Password

          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -name "fDenyTSConnections" -Value 0
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -name "UserAuthentication" -Value 0

          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"

          Set-Service -Name "TermService" -StartupType Automatic
          Start-Service -Name "TermService"

      - name: Optimize Windows 2025 Performance & Power Consumption
        shell: powershell
        run: |
          # 1. Disable Windows Defender to boost CPU and Disk I/O speed
          Set-MpPreference -DisableRealtimeMonitoring $true -ErrorAction SilentlyContinue
          Set-MpPreference -DisableBehaviorMonitoring $true -ErrorAction SilentlyContinue

          # 2. Force High Performance Power Plan (unthrottled CPU speed)
          powercfg /setactive 8c5e7fda-e8bf-4a96-9a85-a6e23a8c635c

          # 3. Disable heavy background services (Search, Update, Delivery Optimization, Superfetch, Telemetry)
          $Services = @("WSearch", "wuauserv", "DoSvc", "SysMain", "DiagTrack")
          foreach ($Service in $Services) {
              Stop-Service -Name $Service -Force -ErrorAction SilentlyContinue
              Set-Service -Name $Service -StartupType Disabled -ErrorAction SilentlyContinue
          }

          # 4. Limit RDP Color Depth to 16-bit (Saves huge data and battery/rendering power on your phone/PC!)
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -name "ColorDepth" -Value 3 -ErrorAction SilentlyContinue

          # 5. Disable Visual Effects on Host
          $Path = "HKCU:\Software\Microsoft\Windows\CurrentVersion\Explorer\VisualEffects"
          if (-not (Test-Path $Path)) { New-Item -Path $Path -Force | Out-Null }
          Set-ItemProperty -Path $Path -Name "VisualFXSetting" -Value 2 -ErrorAction SilentlyContinue

      - name: Launch Tunnel and Connect on Port 443
        shell: powershell
        run: |
          # تشغيل نفق الاتصال في الخلفية لربط منفذ الـ RDP (3389) عبر منفذ الويب الآمن 443
          Start-Process -FilePath "cmd.exe" -ArgumentList "/c ssh -T -p 443 -o StrictHostKeyChecking=no -o UserKnownHostsFile=NUL -o ServerAliveInterval=30 -R 0:127.0.0.1:3389 tcp@free.pinggy.io > pinggy.log 2> pinggy_err.log" -WindowStyle Hidden
          Start-Sleep -Seconds 12

          # قراءة السجلات واستخراج عنوان ومنفذ الاتصال الجديد تلقائياً
          $log = Get-Content -Path "pinggy.log" -ErrorAction SilentlyContinue
          $tcp_line = $log | Select-String -Pattern "tcp://[a-zA-Z0-9.-]+\.pinggy\.link:[0-9]+|tcp://[a-zA-Z0-9.-]+\.pinggy-free\.link:[0-9]+|tcp://[a-zA-Z0-9.-]+\.free\.pinggy\.net:[0-9]+" | ForEach-Object { $_.Matches.Value }
          
          if ($tcp_line) {
              $tcp_clean = $tcp_line -replace "tcp://", ""
              Write-Output "========================================================"
              Write-Output "SUCCESS: Windows Server is ready for remote connection!"
              Write-Output "========================================================"
              Write-Output "Connection details for your RDP Client (PC or Mobile):"
              Write-Output "   - Computer Address: $tcp_clean"
              Write-Output "   - Username: runneradmin"
              Write-Output "   - Password: WindowsPassword123!"
              Write-Output "========================================================"
          } else {
              Write-Output "ERROR: Failed to automatically fetch connection address."
              Write-Output "Dump of pinggy.log:"
              Get-Content -Path "pinggy.log" -ErrorAction SilentlyContinue
              Write-Output "Dump of pinggy_err.log:"
              Get-Content -Path "pinggy_err.log" -ErrorAction SilentlyContinue
          }

          Write-Output "Keeping Windows Server active..."
          Start-Sleep -Seconds 21600
