name: Run HTML5 Windows Browser VNC

on: [workflow_dispatch]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Install and Configure TightVNC Server
        shell: powershell
        run: |
          Invoke-WebRequest -Uri "https://www.tightvnc.com/download/2.8.81/tightvnc-2.8.81-gpl-setup-64bit.msi" -OutFile "tightvnc.msi"
          
          # تم دمج تفعيل الـ Loopback برمجياً أثناء التثبيت لفتح البث لصفحة الويب مباشرة
          Start-Process -FilePath "msiexec.exe" -ArgumentList "/i tightvnc.msi /quiet /norestart ADDLOCAL=Server SERVER_REGISTER_AS_SERVICE=1 SERVER_ADD_FIREWALL_EXCEPTION=1 SET_USEVNCAUTHENTICATION=1 VALUE_OF_USEVNCAUTHENTICATION=1 SET_PASSWORD=1 VALUE_OF_PASSWORD=password SET_USECONTROLAUTHENTICATION=1 VALUE_OF_USECONTROLAUTHENTICATION=1 SET_CONTROLPASSWORD=1 VALUE_OF_CONTROLPASSWORD=password SET_ALLOWLOOPBACK=1 VALUE_OF_ALLOWLOOPBACK=1" -Wait

          Start-Service -Name "tvnserver" -ErrorAction SilentlyContinue

      - name: Optimize Windows Performance
        shell: powershell
        run: |
          Set-MpPreference -DisableRealtimeMonitoring $true -ErrorAction SilentlyContinue
          Set-MpPreference -DisableBehaviorMonitoring $true -ErrorAction SilentlyContinue

          powercfg /setactive 8c5e7fda-e8bf-4a96-9a85-a6e23a8c635c

          $Services = @("WSearch", "wuauserv", "DoSvc", "SysMain", "DiagTrack")
          foreach ($Service in $Services) {
              Stop-Service -Name $Service -Force -ErrorAction SilentlyContinue
              Set-Service -Name $Service -StartupType Disabled -ErrorAction SilentlyContinue
          }

      - name: Launch noVNC and Tunnel
        shell: powershell
        run: |
          Invoke-WebRequest -Uri "https://github.com/pgaskin/easy-novnc/releases/download/v1.1.0/easy-novnc_windows.exe" -OutFile "easy-novnc.exe"

          Invoke-WebRequest -Uri "https://github.com/ekzhang/bore/releases/download/v0.5.1/bore-v0.5.1-x86_64-pc-windows-msvc.zip" -OutFile "bore.zip"
          Expand-Archive -Path "bore.zip" -DestinationPath "."

          Start-Process -FilePath ".\easy-novnc.exe" -ArgumentList "--addr :6080 --host localhost --port 5900" -WindowStyle Hidden

          Start-Process -FilePath ".\bore.exe" -ArgumentList "local 6080 --to bore.pub" -RedirectStandardOutput "bore.log" -RedirectStandardError "bore_err.log" -WindowStyle Hidden

          $tcp_address = $null
          for ($i = 0; $i -lt 15; $i++) {
              Start-Sleep -Seconds 2
              $log = Get-Content -Path "bore.log" -ErrorAction SilentlyContinue
              $tcp_address = $log | Select-String -Pattern "bore.pub:[0-9]+" | ForEach-Object { $_.Matches.Value }
              if ($tcp_address) {
                  break
              }
          }

          if ($tcp_address) {
              $http_address = $tcp_address -replace "bore.pub:", "http://bore.pub:"
              Write-Output "========================================================"
              Write-Output "SUCCESS: Windows Browser VNC is ready!"
              Write-Output "========================================================"
              Write-Output "Click the link below to open Windows directly in your browser:"
              Write-Output "   👉 $http_address/?resize=scale&autoconnect=1&password=password"
              Write-Output "========================================================"
          } else {
              Write-Output "ERROR: Failed to automatically fetch connection address."
              Write-Output "Dump of bore.log:"
              Get-Content -Path "bore.log" -ErrorAction SilentlyContinue
              Write-Output "Dump of bore_err.log:"
              Get-Content -Path "bore_err.log" -ErrorAction SilentlyContinue
          }

          Write-Output "Keeping Windows Server active..."
          Start-Sleep -Seconds 21600
